# ═══════════════════════════════════════════════════════════
# Dotfiles Configuration
# 
# This file controls what gets installed and how it's configured.
# Copy this to .env.local and customize for your setup.
# ═══════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────
# Personal Information
# ───────────────────────────────────────────────────────────

# Git configuration
GIT_USER_NAME="Ezra Gocci"
GIT_USER_EMAIL="ezragocci@gmail.com"

# ───────────────────────────────────────────────────────────
# Repository URL
# ───────────────────────────────────────────────────────────

# Your dotfiles repository (single source of truth)
DOTFILES_REPO_URL="https://github.com/ezra-gocci/dotfiles.git"

# ───────────────────────────────────────────────────────────
# Installation Components
# Set to "true" to install, "false" to skip
# ───────────────────────────────────────────────────────────

# Core Tools
INSTALL_HOMEBREW="true"
INSTALL_GIT="true"
INSTALL_MISE="true"

# Terminal & Shell
INSTALL_WEZTERM="true"
INSTALL_STARSHIP="true"
INSTALL_ZSH_PLUGINS="true"  # autosuggestions, syntax-highlighting
INSTALL_FZF="true"

# Modern CLI Tools (Rust-based)
INSTALL_EZA="true"          # Modern ls
INSTALL_FD="true"           # Modern find
INSTALL_RIPGREP="true"      # Modern grep
INSTALL_BAT="true"          # Modern cat
INSTALL_ZOXIDE="true"       # Smart cd
INSTALL_BOTTOM="true"       # System monitor
INSTALL_PROCS="true"        # Modern ps
INSTALL_DUST="true"         # Disk usage
INSTALL_DUF="true"          # Disk free
INSTALL_DELTA="true"        # Git diff
INSTALL_TOKEI="true"        # Code statistics
INSTALL_HYPERFINE="true"    # Benchmarking
INSTALL_TEALDEER="true"     # tldr pages
INSTALL_SD="true"           # Modern sed
INSTALL_HEXYL="true"        # Hex viewer
INSTALL_GPING="true"        # Ping with graph
INSTALL_BANDWHICH="true"    # Network monitor
INSTALL_DOG="true"          # Modern dig
INSTALL_CHOOSE="true"       # Modern cut/awk
INSTALL_BROOT="true"        # Directory navigator

# Media & Downloads
INSTALL_FFMPEG="true"       # Audio/video processing
INSTALL_YT_DLP="true"       # Video downloader
INSTALL_TRANSMISSION="true" # BitTorrent client

# Data Processing
INSTALL_JQ="true"           # JSON processor
INSTALL_YQ="true"           # YAML/JSON/XML processor
INSTALL_SCC="true"          # Code counter

# Archiving
INSTALL_SEVENZIP="true"     # 7-Zip compression
INSTALL_UNAR="true"         # Multi-format unarchiver
INSTALL_ZSTD="true"         # Zstandard compression

# Development Tools
INSTALL_NEOVIM="true"       # Neovim IDE
INSTALL_LAZYGIT="true"      # Git TUI
INSTALL_GH="true"           # GitHub CLI
INSTALL_UV="true"           # Python package manager
INSTALL_D2="true"           # Diagram scripting
INSTALL_HTTPIE="true"       # HTTP client
INSTALL_SAMPLER="true"      # Shell command visualization

# System & Network
INSTALL_BTOP="true"         # Resource monitor
INSTALL_ATUIN="true"        # Shell history
INSTALL_YAZI="true"         # Terminal file manager
INSTALL_TMUX="true"         # Terminal multiplexer
INSTALL_TAILSCALE="true"    # VPN/mesh networking

# Fonts
INSTALL_NERD_FONTS="true"   # Programming fonts with icons

# Build Tools
INSTALL_CMAKE="true"
INSTALL_MAKE="true"

# Shell Tools
INSTALL_SHELLCHECK="true"   # Shell script linter
INSTALL_SHFMT="true"        # Shell formatter

# ───────────────────────────────────────────────────────────
# Dotfile Components
# Set to "true" to install/symlink, "false" to skip
# ───────────────────────────────────────────────────────────

INSTALL_ZSHRC="true"
INSTALL_GITCONFIG="true"
INSTALL_WEZTERM_CONFIG="true"
INSTALL_STARSHIP_CONFIG="true"
INSTALL_NEOVIM_CONFIG="true"
INSTALL_TMUX_CONFIG="true"
INSTALL_ATUIN_CONFIG="true"
INSTALL_BTOP_CONFIG="true"
INSTALL_MISE_CONFIG="true"
INSTALL_ZED_CONFIG="true"
INSTALL_SSH_CONFIG="true"
INSTALL_ITERM2_CONFIG="true"
INSTALL_CLAUDE_CONFIG="true"

# ───────────────────────────────────────────────────────────
# Neovim Configuration
# ───────────────────────────────────────────────────────────

# Choose Neovim setup method:
# "lazyvim"    - Pre-configured LazyVim distribution (recommended)
# "kickstart"  - Minimal kickstart.nvim for learning
# "custom"     - Use nvim/ directory from this repo
# "none"       - Skip Neovim configuration
NEOVIM_SETUP="custom"

# Auto-install language support in LazyVim
# Space-separated list: "python typescript rust go json yaml markdown"
NEOVIM_LANGUAGES="python typescript rust go json yaml markdown"

# ───────────────────────────────────────────────────────────
# macOS Settings
# ───────────────────────────────────────────────────────────

# Apply macOS defaults (System Preferences tweaks)
APPLY_MACOS_DEFAULTS="true"

# Configure Dock (remove defaults, add preferred apps)
CONFIGURE_DOCK="true"

# Hostname (set ComputerName, HostName, LocalHostName)
HOSTNAME_NAME="masina"

# ───────────────────────────────────────────────────────────
# Advanced Options
# ───────────────────────────────────────────────────────────

# Backup existing dotfiles before installation
BACKUP_EXISTING="true"

# Skip confirmation prompts (useful for automated installs)
SKIP_PROMPTS="false"

# Verbose output (show detailed installation steps)
VERBOSE="false"

# Dry run (show what would be installed without actually installing)
DRY_RUN="false"

# ───────────────────────────────────────────────────────────
# Post-Installation
# ───────────────────────────────────────────────────────────

# Open documentation in browser after installation
OPEN_DOCS_AFTER_INSTALL="true"

# Launch WezTerm after installation
LAUNCH_WEZTERM_AFTER_INSTALL="false"

# ═══════════════════════════════════════════════════════════
# End of Configuration
# ═══════════════════════════════════════════════════════════

# NOTES:
#
# 1. Copy this file to .env.local and customize it:
#    cp .env .env.local
#
# 2. The install script will read .env.local if it exists,
#    otherwise it will use this .env file.
#
# 3. Never commit .env.local to git (it's in .gitignore)
#
# 4. Run backup before factory reset:
#    ./scripts/backup.sh
#
# 5. Restore after fresh install:
#    ./install.sh
